# SPEC-API — HTTP API（基本仕様書）

担当 Issue: #5

## ゴール

インデックスを HTTP で提供する。`127.0.0.1` のみに bind し、認証は設けない（ローカルの個人ログを扱うため外部公開しない）。

## できること（着手時に記入）

Issue #5 着手時に、提供する機能を利用者の言葉で記入する。

## 人間の確認方法

curl でエンドポイントを叩いた実際のレスポンスを確認する（Issue #5 着手時に具体化する）。

エンドポイント表・受け入れ基準は [docs/design/SPEC-API.md](../design/SPEC-API.md)。
